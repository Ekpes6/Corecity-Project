server:
  port: 8080

spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      routes:
        - id: user-service
          uri: ${USER_SERVICE_URI:http://localhost:8081}
          predicates:
            - Path=/api/v1/auth/**, /api/v1/users/**, /api/v1/subscriptions/**, /api/v1/agents/**
          filters:
            - name: AuthFilter
              args:
                excludedPaths: /api/v1/auth/login,/api/v1/auth/register,/api/v1/auth/refresh,/api/v1/auth/health,/api/v1/users/check-phone,/api/v1/subscriptions/plans,/api/v1/agents/*/reputation,/api/v1/subscriptions/webhook/paystack
            - name: CircuitBreaker
              args:
                name: user-service-cb
                fallbackUri: forward:/fallback

        - id: property-service
          uri: ${PROPERTY_SERVICE_URI:http://localhost:8082}
          predicates:
            - Path=/api/v1/properties/**, /api/v1/states/**, /api/v1/enquiries/**, /api/v1/reservations/**
          filters:
            - name: AuthFilter
              args:
                excludedPaths: /api/v1/reservations/webhook/paystack
            - name: CircuitBreaker
              args:
                name: property-service-cb
                fallbackUri: forward:/fallback

        - id: file-service
          uri: ${FILE_SERVICE_URI:http://localhost:8083}
          predicates:
            - Path=/api/v1/files/**
          filters:
            - name: AuthFilter
            - name: CircuitBreaker
              args:
                name: file-service-cb
                fallbackUri: forward:/fallback

        - id: transaction-service
          uri: ${TRANSACTION_SERVICE_URI:http://localhost:8084}
          predicates:
            - Path=/api/v1/transactions/**, /api/v1/commissions/**
          filters:
            - name: AuthFilter
              args:
                excludedPaths: /api/v1/transactions/verify,/api/v1/transactions/webhook/paystack
            - name: CircuitBreaker
              args:
                name: transaction-service-cb
                fallbackUri: forward:/fallback

        - id: notification-service
          uri: ${NOTIFICATION_SERVICE_URI:http://localhost:8085}
          predicates:
            - Path=/api/v1/notifications/**
          filters:
            - name: AuthFilter
            - name: CircuitBreaker
              args:
                name: notification-service-cb
                fallbackUri: forward:/fallback

      globalcors:
        cors-configurations:
          '[/**]':
            allowedOrigins:
              - "http://localhost:3000"
              - "https://corecity.com.ng"
              - "https://www.corecity.com.ng"
            allowedMethods:
              - GET
              - POST
              - PUT
              - DELETE
              - OPTIONS
            allowedHeaders: "*"
            allowCredentials: true

jwt:
  # No default — the application will refuse to start if JWT_SECRET is absent.
  secret: ${JWT_SECRET}
  expiration: 86400000  # 24 hours

resilience4j:
  circuitbreaker:
    configs:
      default:
        slidingWindowSize: 10
        failureRateThreshold: 50
        waitDurationInOpenState: 15s
        permittedNumberOfCallsInHalfOpenState: 5
        automaticTransitionFromOpenToHalfOpenEnabled: true
    instances:
      user-service-cb:
        baseConfig: default
      property-service-cb:
        baseConfig: default
      file-service-cb:
        baseConfig: default
      transaction-service-cb:
        baseConfig: default
      notification-service-cb:
        baseConfig: default

management:
  server:
    port: 9090  # Not published in docker-compose — actuator unreachable from outside
  endpoints:
    web:
      exposure:
        include: health
